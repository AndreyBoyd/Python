# перевод секунд в часы, минуты и секунды

# запрос времени в секундах
time_in_seconds = int(input("Введите время в секундах: "))

# преобразуем секунды в часы (учитавыем что в сутках 24 часа (% 24)), минуты, секунды
hours = (time_in_seconds // 3600) % 24
minutes = (time_in_seconds // 60) % 60
seconds = time_in_seconds % 60

# добавляем 0 к формату вывода если значение часов, минут или секунд меньше 10
if hours < 10:
    hours = str('0' + str(hours))
else:
    hours = str(hours)
if minutes < 10:
    minutes = str('0' + str(minutes))
else:
    minutes = str(minutes)
if seconds < 10:
    seconds = str('0' + str(seconds))
else:
    seconds = str(seconds)

# выводим результат
# print("Время (чч:мм:сс):", str(hours) + ':' + str(minutes) + ':' + str(seconds))  # альтернативный вариант
print(f"Время (чч:мм:сс): {hours}:{minutes}:{seconds}")  # так вроде лучше
