# нумеруем и выводим слова списка с новой строки

# создаём список на основе вводимой строки
my_str = input("Введите строку из нескольких слов через пробел: ")
my_list = my_str.split(' ')

# выводим список для наглядности, что же получилось
print(f"Вы ввели: {my_list}")

# нумеруем список и выводим каждое слово с новой строки
for i, el in enumerate(my_list, 1):
    if len(el) > 10:  # ограничиваем длину слова 10 буквами
        el = el[0:10]
    print(f"{i}. - {el}")
