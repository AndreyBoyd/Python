# вычисление суммы чисел n + nn + nnn

n = int(input("Введите целое число, n: "))

# представляем числа в виде строки и суммируем
sum_n_nn_nnn = (n + int(str(n) + str(n)) + int(str(n) + str(n) + str(n)))

# выводим результат
print(f"Сумма (n + nn + nnn): {sum_n_nn_nnn}")
